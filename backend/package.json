{
	"name": "tamed-stripe-backend",
	"version": "1.0.31",
	"description": "The backend library for the tamed-stripe",
	"repository": "https://github.com/MehmetKaplan/tamed-stripe",
	"main": "tamed-stripe-backend.js",
	"author": "Mehmet Kaplan",
	"license": "MIT",
	"private": false,
	"scripts": {
		"test": "jest --runInBand --coverage --no-cache --forceExit "
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-env": "^1.7.0",
		"jest": "^29.4.2",
		"superagent": "^8.0.9",
		"supertest": "^6.3.3"
	},
	"dependencies": {
		"stripe": "^11.9.1",
		"tamed-pg": "^2.0.0",
		"tick-log": "^1.1.4"
	}
}
